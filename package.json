{
  "name": "526MagentaCrow",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "@adobe/aio-sdk": "^3.0.0",
    "@adobe/commerce-events-ext-tpl": "^1.0.0-beta.9",
    "@adobe/generator-app-events-generic": "^2.0.0",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "@openwhisk/wskdebug": "^1.3.0",
    "jest": "^29"
  },
  "scripts": {
    "test": "jest --passWithNoTests ./test",
    "e2e": "jest --collectCoverage=false --testRegex ./e2e"
  },
  "engines": {
    "node": ">=18"
  },
  "aio-app-builder-templates": [
    "@adobe/generator-app-events-generic",
    "@adobe/commerce-events-ext-tpl"
  ],
  "description": "events triggering from magento to appbuilder"
}
